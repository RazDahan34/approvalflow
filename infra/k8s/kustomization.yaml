apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 00-namespace.yaml
  - 10-infrastructure.yaml
  - 20-dapr.yaml
  - 30-services.yaml
  - 40-config-seed.yaml

# The policy-doc ConfigMap is created from the repo's policy.md by the runbook
# (kustomize's load restrictor forbids ../../ file references — see README).
