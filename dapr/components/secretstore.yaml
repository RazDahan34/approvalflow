apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: secretstore
spec:
  # Reads secrets (e.g. the LLM API key) from the sidecar's environment.
  # Swap for a vault component in production without touching service code (M5/M15).
  type: secretstores.local.env
  version: v1
