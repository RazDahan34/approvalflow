# Local development & test tooling. Install with:
#   pip install -r requirements-dev.txt
pytest>=8
httpx>=0.27
ruff>=0.6
mypy>=1.11
# the shared library, editable, so tests import the live source
-e ./libs/approvalflow_common
